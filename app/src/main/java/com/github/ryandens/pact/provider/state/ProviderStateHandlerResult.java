package com.github.ryandens.pact.provider.state;

/**
 * Describes the result of a {@link ProviderStateHandler} attempting to put the the provider in a
 * given state
 */
enum ProviderStateHandlerResult {
  SUCCESS,
  FAILURE
}
